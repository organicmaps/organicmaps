<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:orientation="vertical">
  <ImageView
    android:id="@+id/iv__guide"
    android:layout_width="match_parent"
    android:layout_height="@dimen/guide_image_size"
    tools:src="@drawable/bg_ads" />
  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="@dimen/description_guide_block_size"
    android:backgroundTint="@color/white_primary"
    android:orientation="vertical"
    android:paddingStart="@dimen/margin_base"
    android:paddingEnd="@dimen/margin_base">
    <TextView
      android:id="@+id/tv__guides_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="@dimen/margin_base"
      android:textAlignment="viewStart"
      android:textColor="?actionMenuTextColor"
      android:textAppearance="@style/TextAppearance.MdcTypographyStyles.Headline4"
      tools:text="@string/guides" />
    <TextView
      android:id="@+id/btn__description"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="@dimen/margin_half_plus"
      android:fontFamily="@string/robotoMedium"
      android:text="@string/description_guide"
      android:textAllCaps="true"
      android:textAppearance="@style/MwmTextAppearance.Body3"
      android:textColor="?colorAccent" />
  </LinearLayout>
  <include
    layout="@layout/divider_horizontal"
    android:layout_width="match_parent"
    android:layout_height="@dimen/divider_height"
    android:layout_marginStart="@dimen/margin_base"
    android:layout_marginEnd="@dimen/margin_base" />
  <LinearLayout
    android:id="@+id/content_by_layout"
    android:layout_width="match_parent"
    android:layout_height="@dimen/block_content_by_size"
    android:orientation="horizontal">
    <ImageView
      android:id="@+id/iv__logo"
      android:layout_width="@dimen/content_by_logo_size"
      android:layout_height="match_parent"
      android:paddingStart="@dimen/margin_base"
      android:paddingTop="@dimen/margin_half_plus"
      android:paddingEnd="@dimen/margin_base"
      android:paddingBottom="@dimen/margin_half_plus"
      tools:src="@drawable/bg_ads" />
    <TextView
      android:id="@+id/tv__content_by"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_marginEnd="@dimen/margin_base"
      android:gravity="center_vertical"
      android:textAlignment="viewStart"
      android:textAppearance="@style/TextAppearance.MdcTypographyStyles.Caption"
      tools:text="@string/about_menu_title" />
  </LinearLayout>
  <include layout="@layout/divider_horizontal" />
</LinearLayout>