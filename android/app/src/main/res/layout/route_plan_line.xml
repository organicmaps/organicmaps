<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/menu_route"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:orientation="vertical">

  <!--    <View-->
  <!--        android:id="@+id/drag_handle"-->
  <!--        android:layout_width="32dp"-->
  <!--        android:layout_height="4dp"-->
  <!--        android:layout_gravity="center_horizontal"-->
  <!--        android:layout_marginTop="8dp"-->
  <!--        android:layout_marginBottom="8dp"-->
  <!--        android:background="?attr/iconTint"-->
  <!--        android:alpha="0.10" />-->

  <include
    android:id="@+id/pull_icon_container"
    layout="@layout/bottom_sheet_handle" />

  <include
    android:id="@+id/routing_types_frame"
    layout="@layout/routing_plan_header"
    tools:layout_height="wrap_content"
    tools:layout_width="match_parent" />

  <LinearLayout
    android:id="@+id/routing_details"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <include layout="@layout/altitude_chart_panel" />

  </LinearLayout>

  <LinearLayout
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.core.widget.NestedScrollView
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:fadeScrollbars="true"
      android:scrollbarSize="@dimen/scrollbar_size"
      android:scrollbarStyle="insideInset"
      android:scrollbarThumbVertical="?android:attr/textColorSecondary"
      android:scrollbars="vertical"
      android:paddingStart="@dimen/margin_base"
      android:paddingEnd="@dimen/margin_half_plus">

      <include
        android:id="@+id/manage_route_panel"
        layout="@layout/manage_route_bottom_sheet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    </androidx.core.widget.NestedScrollView>

  </LinearLayout>

  <TextView
    android:id="@+id/error"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/margin_base"
    android:paddingTop="@dimen/margin_half_plus"
    android:paddingEnd="@dimen/margin_base"
    android:paddingBottom="@dimen/margin_half_plus"
    android:textAppearance="@style/MwmTextAppearance.Body3"
    android:visibility="gone" />
</LinearLayout>
