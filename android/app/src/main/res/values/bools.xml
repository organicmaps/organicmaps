<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors which layout_nav.xml variant is inflated (base bucket = portrait phone).
     Keep this in the same qualifier buckets as layout_nav.xml so that code branching on
     it can never diverge from the layout the resource system actually chose. -->
<resources>
  <bool name="nav_speed_limit_beside_card">false</bool>
</resources>
