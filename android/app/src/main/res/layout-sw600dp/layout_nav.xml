<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/navigation_frame"
  android:layout_width="match_parent"
  android:layout_height="match_parent">

  <androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/nav_top_frame"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Tablets use this layout in BOTH orientations: the sw600dp qualifier outranks
         orientation, so layout-land/ only ever applies to phones.
         Maneuver card: fixed width matching the bottom sheet. -->
    <!-- Only a top gap: the side inset comes from nav_top_frame's padding (shared with the sheet
         and nav-bar strip via startMargin), so a horizontal margin here would double it. -->
    <app.organicmaps.routing.ManeuverView
      android:id="@+id/maneuver_view"
      android:layout_width="@dimen/nav_menu_landscape_width"
      android:layout_height="wrap_content"
      android:layout_marginVertical="@dimen/margin_half"
      android:elevation="@dimen/nav_elevation"
      android:clickable="true"
      app:layout_constraintTop_toTopOf="parent"
      app:layout_constraintStart_toStartOf="parent"/>

    <!-- Speed limit sign: anchored below the maneuver card, left-aligned -->
    <app.organicmaps.sdk.widgets.speedlimit.SpeedLimitView
      android:id="@+id/nav_speed_limit"
      style="@style/MwmWidget.SpeedLimit"
      android:layout_width="60dp"
      android:layout_height="60dp"
      android:layout_margin="@dimen/margin_half"
      app:layout_constraintStart_toStartOf="@id/maneuver_view"
      app:layout_constraintTop_toBottomOf="@id/maneuver_view"/>
  </androidx.constraintlayout.widget.ConstraintLayout>

  <androidx.coordinatorlayout.widget.CoordinatorLayout
    android:id="@+id/nav_bottom_sheet_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
      android:id="@+id/nav_bottom_sheet_background"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:background="#99000000"
      android:clickable="true"
      android:focusable="true"
      android:visibility="gone"
      tools:visibility="visible" />

    <FrameLayout
      android:id="@+id/nav_bottom_sheet"
      style="?attr/bottomSheetStyle"
      android:layout_width="@dimen/nav_menu_landscape_width"
      android:layout_height="wrap_content"
      android:layout_gravity="start"
      app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

      <include layout="@layout/layout_nav_bottom" />
    </FrameLayout>

  </androidx.coordinatorlayout.widget.CoordinatorLayout>

  <View
    android:id="@+id/nav_bottom_sheet_nav_bar"
    android:layout_width="0dp"
    android:layout_height="0dp"
    android:layout_gravity="bottom|start"
    android:background="?cardBackground"/>
</FrameLayout>
