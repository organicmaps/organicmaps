package app.organicmaps.sdk.bookmarks.data;

public enum RoadWarningMarkType
{
  // The order must match the native RoadWarningMarkType enum (libs/map/routing_mark.hpp).
  TOLL,
  FERRY,
  DIRTY,
  STEPS,
  GATE,
  LIFT_GATE,
  UNKNOWN
}
