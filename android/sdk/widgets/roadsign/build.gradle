plugins {
  id 'com.android.library'
}

apply from: "${rootProject.rootDir}/groovy/common-config.gradle"

android {
  namespace 'app.organicmaps.sdk.widgets.roadsign'
}

dependencies {
  implementation 'androidx.annotation:annotation:1.9.1'
}
