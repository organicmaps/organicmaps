/* Keep here changes of the base map style file only. */
@import("../../default/dark/colors.mapcss");

/* Dark-theme variant of the cycling accent. Brighter so the line still
   reads against the dark base. */
@cycleway: #4FC3F7;
